/* ============================================
   audio.js — Sound Manager (placeholder)
   ============================================ */
const Audio = {
  enabled: true,

  init() {
    // Will load audio files when available
  },

  playSFX(name) {
    // Placeholder: click, puzzle_solve, door_open, key_pickup, wrong_code
    console.log(`[SFX] ${name}`);
  },

  playBGM(track) {
    console.log(`[BGM] ${track}`);
  },

  stopBGM() {
    console.log('[BGM] stopped');
  }
};
