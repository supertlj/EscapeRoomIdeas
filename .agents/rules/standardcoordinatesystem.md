---
trigger: always_on
---

All sprite positions and hitboxes MUST be defined within the 1080x1920 design resolution. Use the SpriteManager.ART_SIZE (1024x1024) and ART_Y offset (400) logic to ensure all interactions remain pixel-perfect across different device screen sizes.